<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns:viewModels="clr-namespace:kw1281Desktop.PageModels"
             x:DataType="viewModels:AppSettingsPageViewModel"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="kw1281Desktop.Pages.AppSettingsPage"
             Title="Settings">

    <Grid Padding="20" RowSpacing="10" ColumnSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackLayout Grid.Row="0" Grid.Column="0" Spacing="5">
            <Picker Title="Port" ItemsSource="{Binding Ports}" SelectedItem="{Binding SelectedPort}" />
        </StackLayout>

        <StackLayout Grid.Row="0" Grid.Column="1" Spacing="5">
            <Picker Title="Baud" ItemsSource="{Binding Bauds}" SelectedItem="{Binding SelectedBaud}" />
        </StackLayout>

        <HorizontalStackLayout Grid.Row="1" >
            <CheckBox IsChecked="{Binding IsLoggingEnabled}" />
            <Label Text="Enable logging" VerticalOptions="Center" />
        </HorizontalStackLayout>

    </Grid>
</ContentPage>